package Week7.Singleton;

public class Driver {

	private Singleton testSingleton;
        public static void main(String args[])
        {
          int count = 0;
           Singleton Singleton1 + null;
           while (count = 0) {
                singleton1.getInstance();
                count++;    
            }
         System.out.println("Instances: " + Singleton1.getCounter());

public getInstance(Singleton Singleton)
    {
        Singleton1 = Singleton;
    }
  }
}
